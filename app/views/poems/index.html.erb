<div class="header">
  <div class="header_left">
    <h1><%= t('.all_poems') %></h1>
  </div>
  <div class="header_right">
    <ul class="header_menu">
      <li><%= link_to t('.new'), new_poem_path, :class => 'link' %></li>
    </ul>
  </div>
</div>
<div class="content2">
  <% @poems.each do |p| %>
      <%= link_to p, p, :class => 'link' %>
  <% end %>
</div>
